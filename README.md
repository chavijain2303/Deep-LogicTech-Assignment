# Deep-LogicTech-Assignment
This project provides a simple API that extracts the latest six stories from Time.com by parsing the HTML page directly, without using external libraries. It returns a JSON array with the title and link of each story.
